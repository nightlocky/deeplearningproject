{
  "model_name": "best_unet_latent_mlp",
  "experiment_name": "best_model_tuning",
  "description": "Template metadata for loading the best tuned autoencoder + latent MLP pipeline.",
  "artifacts": {
    "ae_weights_path": "src/models/best_model_tuning/best_model_tuning/loss_functions/0.5/loss_functions_0.5_best_ae.pth",
    "mlp_weights_path": "REPLACE_WITH_LOCAL_PATH_TO_best_final_mlp.pth"
  },
  "architecture": {
    "autoencoder_type": "smp.Unet",
    "backbone": "resnet50",
    "encoder_weights": null,
    "in_channels": 1,
    "classes": 1,
    "activation": "sigmoid",
    "pooling": "AdaptiveAvgPool2d((1, 1))",
    "latent_dim": 2048,
    "mlp_hidden_layers": [1024, 512, 256],
    "mlp_dropout": 0.3
  },
  "preprocessing": {
    "image_size": 224,
    "color_mode": "grayscale",
    "tensor_shape": "1x224x224",
    "resize": [224, 224],
    "normalization": "ToTensor only"
  },
  "training": {
    "encoder_lr": 0.0001,
    "decoder_lr": 0.001,
    "ae_epochs": 30,
    "mlp_epochs": 30,
    "loss_summary": "UNet autoencoder trained with weighted SSIM + L1 during tuning. MLP trained with BCELoss on latent embeddings.",
    "best_alpha": 0.5
  },
  "inference": {
    "threshold": "REPLACE_WITH_SELECTED_THRESHOLD",
    "score_type": "mlp_probability",
    "decision_rule": "predict anomaly when score > threshold"
  },
  "notes": [
    "Copy the RunPod best_final_mlp.pth to a persistent local path and update mlp_weights_path.",
    "Fill in the final threshold from the winning run before using this metadata for reproducible inference."
  ]
}
