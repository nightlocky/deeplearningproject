{
  "model_name": "best_unet_latent_mlp",
  "experiment_name": "best_model_tuning",
  "description": "Best tuned U-Net autoencoder plus latent MLP recovered from the winning MLflow run.",
  "artifacts": {
    "ae_weights_path": "src/inference/best_model/loss_functions_0.5_best_ae.pth",
    "mlp_weights_path": "src/inference/best_model/best_final_mlp.pth"
  },
  "architecture": {
    "autoencoder_type": "smp.Unet",
    "backbone": "resnet50",
    "encoder_weights": null,
    "in_channels": 1,
    "classes": 1,
    "activation": "sigmoid",
    "pooling": "AdaptiveAvgPool2d((1, 1))",
    "latent_dim": 2048,
    "mlp_hidden_layers": [256, 64],
    "mlp_dropout": 0.3
  },
  "preprocessing": {
    "image_size": 224,
    "color_mode": "grayscale",
    "tensor_shape": "1x224x224",
    "resize": [224, 224],
    "normalization": "ToTensor only"
  },
  "training": {
    "encoder_lr": 0.0001,
    "decoder_lr": 0.001,
    "ae_epochs": 30,
    "mlp_epochs": 30,
    "loss_summary": "U-Net autoencoder trained with weighted SSIM plus L1 during tuning. MLP trained with BCELoss on latent embeddings.",
    "best_alpha": 0.5
  },
  "inference": {
    "threshold": 0.3208799362182617,
    "score_type": "mlp_probability",
    "decision_rule": "predict anomaly when score > threshold"
  },
  "notes": [
    "Saved model files are colocated with this metadata inside src/inference/best_model.",
    "This metadata reflects the MLflow-winning configuration: MLP/256_64_drop_0.3."
  ]
}
